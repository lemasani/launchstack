{
  "name": "launchstack-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter launchstack dev",
    "build": "pnpm --filter launchstack build",
    "build:template": "cd packages/template && tsc -b && vite build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
